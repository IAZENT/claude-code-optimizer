---
description: "Switch to deep/high-effort mode for this session. Use for complex architecture, hard bugs, multi-file refactors."
---
DEEP MODE ON for this session:
1. Write the word `high` to `.claude/.session/effort.txt` (create directories if needed).
2. Say exactly this and nothing else:
"Running in **high-effort mode**. Full architectural reasoning + subagents allowed.
For maximum reasoning, please run: \`export MAX_THINKING_TOKENS=10000\` in your terminal before continuing."
