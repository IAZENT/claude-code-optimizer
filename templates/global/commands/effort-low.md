---
description: "Switch to lean/low-effort mode for this session. Use for simple edits, lookups, renames, boilerplate. Saves 80-90% reasoning tokens."
---
LEAN MODE ON for this session:
1. Write the word `low` to `.claude/.session/effort.txt` (create directories if needed).
2. Say exactly this and nothing else:
"Running in **low-effort mode**. CoD + no subagents + direct answers. 
For maximum savings, please run: `export MAX_THINKING_TOKENS=0` in your terminal before continuing."

Signal lean mode with "[lean]" on your next response.
